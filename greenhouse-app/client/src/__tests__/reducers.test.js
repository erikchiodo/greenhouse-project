import { reducer } from '../utils/reducers';
import {
  //TODO: Add reducer functions from utils/actions folder
} from '../utils/actions';

// TODO: Create const object called Initial State that has following properities: product (empty array), cart (array with products -- id, name, purchaseQuantity), cartOpen (defaulted to false), categories (subdoc with one property [name]), currentCategory (defaulted to '1')

// Create test case for each functions imported from util/actions folder